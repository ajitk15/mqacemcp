# Base deps shared by every LLM provider. langchain-openai is the DEFAULT
# provider (LLM_PROVIDER=openai). To use Gemini or Claude instead, also install
# the matching overlay (requirements-gemini.txt / requirements-claude.txt);
# scripts/start-all -Setup does this for you based on the provider you pick.
langgraph>=0.2.50
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-mcp-adapters>=0.1.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0

# LLM provider overlay: Google Gemini.
# Install alongside the base deps:  pip install -r requirements.txt -r requirements-gemini.txt
# scripts/start-all -Setup installs this automatically when you pick "gemini".
# Set LLM_PROVIDER=gemini and GOOGLE_API_KEY in agent/.env.
langchain-google-genai>=2.0.0
